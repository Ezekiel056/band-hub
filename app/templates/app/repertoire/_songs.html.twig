    <ul class="flex flex-col gap-6 md:grid md:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4 xl:gap-6">
        {% for song in songs %}
            {{ include('/app/repertoire/_song-card.html.twig',{icon: song.status.icon()}) }}
        {% endfor %}
    </ul>
