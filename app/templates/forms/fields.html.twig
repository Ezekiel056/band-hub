
{# Defintion de de chaque element d'un formulaire #}

{% use 'form_div_layout.html.twig' %}

{% block form_start %}
    {% set attr = attr|merge({'class': (attr.class|default('') ~ ' flex flex-col gap-6')|trim}) %}
    {{ parent() }}
{% endblock %}

{% block form_row %}
  {% if form.vars.block_prefixes|first == 'hidden' %}
    {{ form_widget(form) }}
  {% else %}
    <div class="labeled-input">
      {{ form_label(form) }}
      {{ form_widget(form) }}
      {{ form_errors(form) }}
    </div>
  {% endif %}
{% endblock %}


{% block checkbox_row %}
  <div class="flex flex-col gap-1">
    <div class="flex items-start gap-2">
      {{ form_widget(form) }}
      {{ form_label(form, null, { label_attr: { class: ('cursor-pointer ' ~ (label_attr.class|default('')))|trim } }) }}
    </div>
    {{ form_errors(form) }}
  </div>
{% endblock %}

{# Defintion de la mise en forme des erreurs #}
{% block form_errors %}
    {% if errors|length > 0 %}
        {% for error in errors %}
            <p class="text-red-500 text-sm mt-1">{{ error.message }}</p>
        {% endfor %}
    {% endif %}
{% endblock %}

{% block form_end %}
  <p class="text-sm italic text-(--text-color)">* Champs obligatoires</p>
  {{ parent() }}
{% endblock %}
